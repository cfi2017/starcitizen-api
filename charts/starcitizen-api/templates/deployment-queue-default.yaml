{{- if .Values.queueDefault.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "starcitizen-api.fullname" . }}-queue-default
  labels:
    {{- include "starcitizen-api.componentLabels" (dict "context" . "component" "queue-default") | nindent 4 }}
  {{- with (include "starcitizen-api.commonAnnotations" .) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.queueDefault.replicaCount }}
  selector:
    matchLabels:
      {{- include "starcitizen-api.componentSelectorLabels" (dict "context" . "component" "queue-default") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "starcitizen-api.componentLabels" (dict "context" . "component" "queue-default") | nindent 8 }}
        {{- with .Values.queueDefault.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- include "starcitizen-api.checksumAnnotations" . | nindent 8 }}
        {{- with .Values.queueDefault.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "starcitizen-api.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.queueDefault.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "starcitizen-api.initContainers" . | nindent 8 }}
      containers:
        - name: queue
          image: {{ include "starcitizen-api.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.queueDefault.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: CONTAINER_ROLE
              value: queue
            - name: QUEUE_NAME
              value: default
            - name: QUEUE_TRIES
              value: {{ .Values.queueDefault.tries | quote }}
            - name: QUEUE_MAX_JOBS
              value: {{ .Values.queueDefault.maxJobs | quote }}
            - name: QUEUE_SLEEP
              value: {{ .Values.queueDefault.sleep | quote }}
            - name: QUEUE_TIMEOUT
              value: {{ .Values.queueDefault.timeout | quote }}
            - name: QUEUE_MEMORY
              value: {{ .Values.queueDefault.memoryLimit | quote }}
            {{- include "starcitizen-api.secretEnv" . | nindent 12 }}
          envFrom:
            {{- include "starcitizen-api.envFrom" . | nindent 12 }}
          {{- with .Values.queueDefault.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- include "starcitizen-api.volumeMounts" . | nindent 12 }}
      volumes:
        {{- include "starcitizen-api.volumes" . | nindent 8 }}
      {{- with .Values.queueDefault.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.queueDefault.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.queueDefault.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
